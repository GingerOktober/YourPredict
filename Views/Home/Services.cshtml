@model IEnumerable<App.Models.Goodss>
@{
    ViewBag.Title = "Услуги";
    Layout = "/Views/Shared/_Layout.cshtml";
}
@using (Html.BeginForm("Services", "Home", FormMethod.Post, new { @class = "form-horizontal", role = "form" }))
{
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <style>
        * {
            box-sizing: border-box;
        }

        #wrapper {
            margin: 0 auto;
            width: 1001px;
            height: 1000px;
        }

        #container {
            margin: auto;
            width: 100%;
            height: 2150px;
        }
        .title
        {
            text-align: center;
            top: 100px;
            color: white;
            position: relative;
        }

        .conts {
            display: flex;
            flex-direction: row;
            flex-wrap: nowrap;
            justify-content: center;
            align-content: stretch;
        }
        .cont01{ /*Гадание на таро*/
            width: 425px;
            height: 470px;
            left: 0px;
            float: left;
            top: 150px;
            position: relative;
            background-color: #322A40;
        }

        .cont02 { /*Гипноз*/
            width: 530px;
            height: 470px;
            margin-left: 465px;
            top: 150px;
            position: relative;
            background-color: #ebe0ff;

        }
        .cont03{/*Приворот*/
            width: 327px;
            height:530px;
            left: 0px;
            float: left;
            top: 200px;
            position: relative;
            background-color: #ebe0ff;
        }

        .cont04 { /*Снять порчу*/
            width: 328px;
            height: 530px;
            margin-left: 30px;
            margin-right: 30px;
            top: 200px;
            position: relative;
            background-color: #322A40;
        }

        .cont05 { /*Изменить судьбу*/
            width: 280px;
            height: 530px;
            float: right;
            top: 200px;
            position: relative;
            background-color: #ebe0ff;
        }

        .cont06 { /*Приворот*/
            width: 300px;
            height: 450px;
            left: 0px;
            float: left;
            top: 250px;
            position: relative;
            background-color: #322A40;
        }

        .cont07 { /*Снять порчу*/
            width: 315px;
            height: 450px;
            margin-left: 30px;
            margin-right: 30px;
            top: 250px;
            position: relative;
            background-color: #ebe0ff;
        }

        .cont08 { /*Изменить судьбу*/
            width: 315px;
            height: 450px;
            float: right;
            top: 250px;
            position: relative;
            background-color: #ebe0ff;
        }

        .cont09 { /*Нанести порчу*/
            width: 100%;
            height: 290px;
            background-color: #211c2b;
            position: relative;
            top: 290px;
            text-align: center;
        }
        .textincont-white
        {
            padding: 10px 30px;
            color: white;
            text-align: start;
            top: 50px;
            font-size: 16px;
            line-height: 25px;
        }
        .textincont-black {
            padding: 10px 30px;
            color: black;
            text-align: start;
            top: 50px;
            font-size: 16px;
            line-height: 25px;
        }
        .btn-light
        {
            border: none;
            outline: none;
            display: inline-block;
            text-align: center;
            text-decoration: none;
            cursor: pointer;
            padding: 16px 28px;
            border-radius: 0px;
            font-family: opensans;
            color: #271447; /*текст кнопки*/
            position: absolute;
            bottom: 10px;
            left: 50%;
            transform: translate(-50%, -50%);
            -ms-transform: translate(-50%, -50%);
            background-color: #d1c7e0; /*Цвет кнопки*/
            font-size: 25px;
        }

        .btn-dark {
            border: none;
            outline: none;
            display: inline-block;
            text-align: center;
            text-decoration: none;
            cursor: pointer;
            padding: 16px 28px;
            border-radius: 0px;
            font-family: opensans;
            color: #d1c7e0; /*текст кнопки*/
            position: absolute;
            bottom: 10px;
            left: 50%;
            transform: translate(-50%, -50%);
            -ms-transform: translate(-50%, -50%);
            background-color: #271447; /*Цвет кнопки*/
            font-size: 25px;
        }

        .btn-light:hover {
            background-color: #786e91;
        }

        .btn-dark:hover {
            background-color: #4f426d;
        }
    </style>
    <title> Узнайте, что ждёт Вас в будущем прямо сейчаc! </title>
<body>
    <div id="wrapper">
        <div id="container">
            <h1 class="title">Наши услуги</h1>
            <div class="cont01">
                <div class="textincont-white">
                        @foreach (var item in Model)
                        {
                            if (item.GoodId == 1)
                            {
                                <h2>@item.Name</h2>
                                <p>@item.Description</p>
                                <form data-goodid="@item.GoodId" method="post">
                                    <input type="hidden" name="goodId" value="@item.GoodId" />
                                    <button type="submit" class="btn-light addToCartButton" data-goodid="@item.GoodId">Заказать</button>
                                </form>
                            }
                        }
                </div>
            </div>
            <div class="cont02">
                <div class="textincont-black">

                        @foreach (var item in Model)
                        {
                            if (item.GoodId == 2)
                            {
                                <h2>@item.Name</h2>
                                <p>@item.Description</p>
                                <form data-goodid="@item.GoodId" method="post">
                                    <input type="hidden" name="goodId" value="@item.GoodId" />
                                    <button type="submit" class="btn-dark addToCartButton" data-goodid="@item.GoodId">Заказать</button>
                                </form>
                            }
                        }
                </div>

            </div>
            <div class="conts">
            <div class="cont03">
                <div class="textincont-black">
                            @foreach (var item in Model)
                            {
                                if (item.GoodId == 3)
                                {
                                    <h2>@item.Name</h2>
                                    <form data-goodid="@item.GoodId" method="post">
                                        <input type="hidden" name="goodId" value="@item.GoodId" />
                                        <button type="submit" class="btn-dark addToCartButton" data-goodid="@item.GoodId">Заказать</button>
                                    </form>
                                    <p>@item.Description</p>
                                }
                            }
                        </div>
            </div>
            <div class="cont04">
                <div class="textincont-white">

                            @foreach (var item in Model)
                            {
                                if (item.GoodId == 4)
                                {
                                    <h2>@item.Name</h2>
                                    <p>@item.Description</p>
                                    <form data-goodid="@item.GoodId" method="post">
                                        <input type="hidden" name="goodId" value="@item.GoodId" />
                                        <button type="submit" class="btn-light addToCartButton" data-goodid="@item.GoodId">Заказать</button>
                                    </form>
                                }
                            }
                    </div>
                </div>
            <div class="cont05">
                <div class="textincont-black">

                            @foreach (var item in Model)
                            {
                                if (item.GoodId == 5)
                                {
                                    <h2>@item.Name</h2>
                                    <p>@item.Description</p>
                                    <form data-goodid="@item.GoodId" method="post">
                                        <input type="hidden" name="goodId" value="@item.GoodId" />
                                        <button type="submit" class="btn-dark addToCartButton" data-goodid="@item.GoodId">Заказать</button>
                                    </form>
                                }
                            }
                            </div>
                </div>
            </div>
            <div class="conts">
            <div class="cont06">
                    <div class="textincont-white">

                            @foreach (var item in Model)
                            {
                                if (item.GoodId == 6)
                                {
                                    <h2>@item.Name</h2>
                                    <p>@item.Description</p>
                                    <form data-goodid="@item.GoodId" method="post">
                                        <input type="hidden" name="goodId" value="@item.GoodId" />
                                        <button type="submit" class="btn-light addToCartButton" data-goodid="@item.GoodId">Заказать</button>
                                    </form>
                                }
                            }
                    </div>
            </div>
            <div class="cont07">
                    <div class="textincont-black">

                            @foreach (var item in Model)
                            {
                                if (item.GoodId == 7)
                                {
                                    <h2>@item.Name</h2>
                                    <p>@item.Description</p>
                                    <form data-goodid="@item.GoodId" method="post">
                                        <input type="hidden" name="goodId" value="@item.GoodId" />
                                        <button type="submit" class="btn-dark addToCartButton" data-goodid="@item.GoodId">Заказать</button>
                                    </form>
                                }
                            }
                    </div>
            </div>
            <div class="cont08">
                    <div class="textincont-black">

                            @foreach (var item in Model)
                            {
                                if (item.GoodId == 8)
                                {
                                    <h2>@item.Name</h2>
                                    <p>@item.Description</p>
                                    <form data-goodid="@item.GoodId" method="post">
                                        <input type="hidden" name="goodId" value="@item.GoodId" />
                                        <button type="submit" class="btn-dark addToCartButton" data-goodid="@item.GoodId">Заказать</button>
                                    </form>
                                }
                            }
                </div>
                    </div>
                </div>
            <div class="cont09">
                <div class="textincont-white">
                        @foreach (var item in Model)
                        {
                            if (item.GoodId == 9)
                            {
                                <h2>@item.Name</h2>
                                <p>@item.Description</p>
                                <form data-goodid="@item.GoodId" method="post">
                                    <input type="hidden" name="goodId" value="@item.GoodId" />
                                    <button type="submit" class="btn-light addToCartButton" data-goodid="@item.GoodId">Заказать</button>
                                </form>
                            }
                        }
                        </div>
                    </div>
            </div>
        </div>
        <script>
            $(document).ready(function () {
                $('.addToCartButton').click(function () {
                    var goodId = $(this).data('goodid');
                    var userId = 1;
                    $.ajax({
                        url: '/Home/AddToCart',
                        type: 'POST',
                        data: { goodId: goodId, userId: userId },
                        success: function (response) {
                            if (response.exists == 0) {
                                window.location.href = '/Home/Registration';
                            }
                            else
                            {
                                window.location.href = '/Home/Services';
                            }
                        }
                    });
                });
            });
            </script>
</body>
}